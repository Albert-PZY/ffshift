# 文案契约 · FFShift

> 所有出口文字都按这份契约写：文档、README、界面文案、提交信息、AI 生成的草稿。
> 自动检查在 `scripts/lib/tone.mjs`（禁词表、祈使句、宽度）；提交时由 commit-msg 钩子执行。
> 现状说明：本文件当前由人工维护。`oil-tone` 在本机不可用（`.agents/skills/` 下只有 `oil-frontend`），
> 一旦可用就把它的规则并进来，替换本文件。

## 三条底线

1. **具体**：写事实、数字、命令、路径，不写感受。
2. **可核对**：每句话都要能被验证——能指出文件、行号、测试结果。
3. **不空话**：说不出改了什么，就说明还没想清楚。

## 禁词表（出现即回退重写）

优化了性能 · 提升了体验 · 完善了功能 · 修复了一些问题 · 代码优化 · 细节优化
相关调整 · 若干优化 · 整体优化 · 各种优化 · 做了一些改动 · update code · fix bug

## 句式

- 提交首行、标题、按钮：**祈使句**——"修复 X"而不是"修复了 X"。
- 一句话讲一件事，中文 ≤40 字；超过就断句或改表格。
- 数字用阿拉伯数字；单位写全（MiB / 毫秒 / 分钟）。
- 一段不超过 4 行；能用表格就不用段落。
- 中文标点用全角，代码与路径用半角反引号包裹。

## 必须区分来源

写技术判断时标明出处，三者不能混：

| 标记 | 含义 |
| --- | --- |
| 【官方】 | 有文档链接可查（GitHub / git / ffmpeg 等） |
| 【惯例】 | 社区约定，不是官方要求 |
| 【本项目】 | 我们自己的取舍，附理由 |

## 术语统一

| 用 | 不用 |
| --- | --- |
| ffmpeg / ffprobe | FFMPEG、FFmpeg（工具名小写） |
| 缩略图 | 预览图、封面图 |
| 档位（更清晰 / 均衡 / 更小） | 模式、等级、profile |
| 转换 / 转码 | 转格式、压制、encode |
| 队列 | 任务列表、队列池 |

## 状态词（全局只用这五个）

排队中 · 转换中 · 已完成 · 失败 · 已取消

## 自检

- [ ] 有没有可以删掉而不丢信息的句子？
- [ ] 是不是在描述"改了什么"而没写"为什么"？
- [ ] 数字、路径、命令是不是真的核对过？
- [ ] 来源标对了吗（官方 / 惯例 / 本项目）？
