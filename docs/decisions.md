# 决策记录（ADR）· FFShift

> 本文件是决策的权威来源，每条记录：场景 / 决策 / 备选 / 理由 / 代价 / 反馈。
> `docs/architecture.md` 只做索引；开发阶段每完成一个模块，在此追加或更新"反馈"。
> 对应笔试题第 4 条：开发者的决策选择与反馈需要记录到文档。

## 索引

| 编号 | 决策 | 状态 |
| --- | --- | --- |
| ADR-001 | 桌面框架主选 Electron（原选 Tauri 2） | 已接受 · 2026-09-22 修订 |
| ADR-002 | ffmpeg 走 CLI 子进程 | 已接受 |
| ADR-003 | 缩略图用 ffmpeg 抽帧 + 本地缓存 | 已接受 |
| ADR-004 | 本地存储先用 JSON 文件 | 已接受 |
| ADR-005 | AI 只输出白名单参数 JSON | 被 ADR-017 取代（整个功能移除） |
| ADR-006 | 项目命名 FFShift（原 FormatShift） | 已接受 · 2026-09-22 改选 |
| ADR-007 | 界面走高级极简（单一强调色 + 反白主按钮） | 已接受 · 布局与按钮样式由 ADR-014 替代，暗色单主题由 ADR-016 替代 |
| ADR-008 | 确立"生产级选型原则"：稳定、低维护、少依赖、可交接 | 已接受 |
| ADR-009 | 强制分支模型：main 受保护，改动一律走分支 + PR | 已接受 |
| ADR-010 | 自动化沉淀分层：自动写事实 / AI 出草稿 / 人工签字 | 已接受 |
| ADR-011 | 引入 OpenSpec 做规格驱动开发 | 已接受 |
| ADR-012 | 文案约束接入 oil-tone（原"自拟契约"决策被取代） | 已接受 |
| ADR-013 | 样式用原生 CSS 变量，不引 Tailwind（偏离 §4.3） | 被 ADR-015 取代 |
| ADR-014 | 采用 v0.app 的三栏工作台布局与配色 | 被 ADR-015 取代（布局与配色部分） |
| ADR-015 | 内化 EnsoCode 的设计语言，改用 Tailwind v4 + Base UI | 已接受 · 替代 ADR-013 与 ADR-014 |
| ADR-016 | 加亮暗两套主题，默认亮色（替代 ADR-007"只发暗色"） | 已接受 |
| ADR-017 | 移除产品内的 AI 参数建议（替代 ADR-005） | 已接受 |
| ADR-018 | 主界面与设置页分工：偏好搬进设置页，主界面只留转换 | 已接受 |
| ADR-019 | 字号调的是根字号；外壳尺寸不跟着走 | 已接受 · 同日修订 |
| ADR-020 | 加浅黑主题；表单底色改成 `--field` 令牌 | 已接受 |

---

## ADR-001 · 桌面框架主选 Electron（原选 Tauri 2）

- **日期**：2026-09-22
- **状态**：已接受；同日修订（主选由 Tauri 2 调整为 Electron）
- **背景**：笔试要求桌面客户端（界面选型与"客户端"关联为 0 不考虑）；3 天单人开发；需求方补充"生产级项目要稳定、维护成本低"。
- **决策**：主选 Electron + React + TypeScript；WPF（.NET 8 LTS）为 C# 熟练者的并列候选；Tauri 2 降为备选。
- **备选**：
  - Tauri 2：体积/内存最优，但生态年轻，Rust 在长期维护与招人上更贵；
  - WPF：Windows 原生能力最强，但仅 Windows、现代化视觉打磨慢；
  - Qt 6 / PySide6 / WinUI 3 / Avalonia / Flutter：各有硬伤（调试成本、打包坑、桌面生态弱）。
- **理由**：Electron 生态十年以上、打包/更新/调试链成熟、生产案例多（VS Code / Slack 等）、JS/TS 人才最多，交接与维护成本最低。
- **代价**：安装包约 150MB、内存占用高——作为产品成本接受，并在面试话术中主动说明。
- **反馈**：初版文档主选 Tauri 2，收到"稳定 + 低维护"要求后调整为 Electron，并固化为原则（ADR-008）。实际表现待开发阶段验证。

## ADR-002 · ffmpeg 走 CLI 子进程

- **日期**：2026-09-22
- **状态**：已接受
- **背景**：题目明确可走 ffmpeg 命令行；产品需要进度、取消、重试、批量。
- **决策**：spawn `ffmpeg.exe`，参数数组传参，`-progress pipe:1 -nostats` 输出结构化进度。
- **备选**：libav* API（编译/跨平台成本高）；第三方封装库（多一层黑盒）；ffmpeg.wasm（无硬件加速、大文件内存爆）。
- **理由**：可控性最好——可取消、可并发、进度是结构化 key=value；数组传参避免 shell 注入与中文/空格路径问题。
- **代价**：自写进度解析与错误映射（已模块化设计，配套单测）。
- **反馈**：待开发阶段验证（重点：中文/空格路径、进度准确性）。

## ADR-003 · 缩略图用 ffmpeg 抽帧 + 本地缓存

- **日期**：2026-09-22
- **状态**：已接受
- **背景**：题目第 1 条要求"获取视频缩略图并显示"。
- **决策**：`ffmpeg -ss <0.1×时长> -frames:v 1 -vf scale=480:-2` 抽帧；缓存键为"路径 + 修改时间 + 大小"的哈希，存应用缓存目录。
- **备选**：WebCodecs 抽帧（兼容性差、失败率高）；九宫格拼图（v1 没必要）；内嵌封面（大多数文件没有，仅作补充）。
- **理由**：格式覆盖全、实现简单、可缓存、失败可降级。
- **代价**：首次导入每个文件多一次进程调用。
- **反馈**：待开发阶段验证（重点：抽帧失败不刷屏、不留大灰块占位）。

## ADR-004 · 本地存储先用 JSON 文件

- **日期**：2026-09-22
- **状态**：已接受
- **背景**：需要保存设置与转换历史，且要求低维护。
- **决策**：JSON 文件（带 `version` 字段），v1 不引数据库。
- **备选**：SQLite（查询强，但多一层配置与迁移成本）；localStorage（容量与一致性差）。
- **理由**：3 天成本最低，符合"依赖最小化"；升级不破坏旧数据（version 字段）。
- **代价**：历史量大后需要迁移方案。
- **反馈**：待定。

## ADR-005 · AI 只输出白名单参数 JSON

- **日期**：2026-09-22
- **状态**：被 ADR-017 取代（2026-09-23：整个"AI 参数建议"功能移除）。这条决策本身没有被推翻——如果以后重做，安全边界照旧。
- **背景**：产品需要"一句话配参数"，但不能让模型直接碰命令行。
- **决策**：LLM 输出仅"预设 + 白名单字段 + 一句解释"的 JSON，Schema 校验通过后才拼接参数；无 Key / 超时 / 校验失败自动降级到规则引擎。
- **备选**：让模型直接生成命令行（不可控、存在注入风险）。
- **理由**：安全、可校验、可降级；视频文件与 Key 不出本机。
- **代价**：表达能力受限（对目标用户足够）。
- **反馈**：实现如实落地了：白名单校验、无 Key 降级、模型不守格式时从散文里提取要点，20 条单测守着。
  但实跑暴露了这条路线本身的脆弱：模型两次不按 JSON 输出（一次长篇反问、一次直接给方案分析），
  最终产品里"真模型"这条路径的稳定性取决于第三方接口，而它对用户的价值并不比界面上的三档预设更高。
  2026-09-23 整个功能按 ADR-017 移除；`docs/architecture.md` §4.6 保留了当时的选型对比供重做时参考。

## ADR-006 · 项目命名 FFShift（原 FormatShift）

- **日期**：2026-09-22（同日改选）
- **状态**：已接受；由"FormatShift"改选为"FFShift"
- **背景**：项目要有能讲清、能查重的名字（提交 GitHub）；需求方补充要求"更极客、更方便记忆"。
- **决策**：英文名 FFShift，仓库 `ffshift`；Slogan"ffmpeg 的换挡键"；一句话说明"不用懂参数，也能转好视频"。
- **备选**：FFPilot（AI 副驾）/ Transcodr（黑话去元音）/ VidMogrify（致敬 ImageMagick）/ FFSmith / FFPipe / FFForge；首轮 FormatShift、VidBridge。
- **理由**：FF 是 ffmpeg 系的极客命名传统，Shift 一眼懂"换挡/切换"；7 字符好记好读，仓库名干净。
- **代价**：GitHub 有 1 个无关同名项目（FFShifter，手柄模拟器，0 star）；旧文档已同步改名。
- **反馈**：排除项留档——VidPipe（225 star 同类）/ Remux（行业通用词 + 549 star 同类）/ Molt（6954 同名）/ Smelt（805 同名）/ VidMagick（与手机 App "VidMagic" 读音相同）。

## ADR-007 · 界面走高级极简

- **日期**：2026-09-22
- **状态**：已接受
- **背景**：需求方要求"尽量高级极简、不要花哨、体验优先、直观清晰"。
- **决策**：近中性暗色 + 单一强调色（蓝）+ 反白主按钮；颜色只表达状态和唯一主按钮；明确不做渐变 / 发光 / 装饰插画 / 滚动特效。
  （2026-09-23：配色与布局先由 ADR-014 换成 v0.app 方案，再随 ADR-015 换成 EnsoCode 的令牌；"只做暗色"这一条由 ADR-016 推翻。立场本身没变。）
- **备选**：多色品牌方案（橙 + 蓝）；亮色优先。
- **理由**：颜色不参与分层，层级靠间距 / 字重 / 边框；减少视觉噪音即减少认知负担。
- **代价**：视觉冲击力弱，靠层次与状态反馈取胜。
- **反馈**：设计系统已按此重写（`docs/design-system.md`），并写入架构文档 §7。

## ADR-008 · 确立"生产级选型原则"

- **日期**：2026-09-22
- **状态**：已接受
- **背景**：需求方要求："生产级项目，尽量使用稳定且维护成本低的技术选型"。
- **决策**：新增架构文档 §4.10 选型原则——选"无聊"的成熟技术、依赖最小化、锁版本、人才可得性、更新可控、数据带 version、可观测、拒绝实验性 API 与黑盒封装；后续每个依赖按此审查。
- **备选**：维持初版"性能 / 体积优先"取向。
- **理由**：长期维护成本 > 短期观感与体积优势。
- **代价**：放弃 Tauri 的体积/内存优势；开发期不能随手引入炫技依赖。
- **反馈**：直接导致 ADR-001 修订；该原则同时适用于代码、依赖与文档工具链。

---

## ADR-009 · 强制分支模型：main 受保护，改动一律走分支 + PR

- **日期**：2026-09-22
- **状态**：已接受
- **背景**：需求方要求按生产模式管理代码：按模块建分支开发，不允许直接在主分支提交，主分支只能由其它分支合并进来。
- **决策**：`main` 是唯一长期分支，只接受 PR 合并；开发走 `feat|fix|refactor|test|docs|chore/<模块>-<动作>`；本地 pre-commit 拦 main 直提、pre-push 拦 main 直推；远程再开分支保护；里程碑在 main 上打注释 tag。
- **备选**：Git Flow（develop / release / hotfix 多长期分支）；主干开发（trunk-based）；只靠文档约定不做工具强制。
- **理由**：单人 3 天项目里 Git Flow 的长期分支是纯开销；只写文档约定等于没约定（工具拦不住的规则一定会破）；GitHub flow 是官方推荐且与 PR 机制天然契合。
- **代价**：每次改动多两步（开分支、开 PR），但换来可追溯的合并记录与干净的主干。
- **反馈**：待开发阶段验证——重点看"小改动是否被分支流程拖慢"，若拖慢则合并文档类改动到单个 `docs/` 分支。

## ADR-010 · 自动化沉淀分层：自动写事实 / AI 出草稿 / 人工签字

- **日期**：2026-09-22
- **状态**：已接受
- **背景**：需求方要求提交时自动把记录沉淀进 `ai-session-log.md`、`testing.md`、`decisions.md`，并同步项目信息密度到 spec。
- **决策**：分三层——① 确定性事实（提交流水、测试登记、项目状态页）自动写入；② 判断性内容（影响面、规格补充建议、风险）由 LLM 出草稿到 `.ffshift/pending/`，人工采纳；③ `decisions.md` 与 `openspec/specs/` 只由人改。
- **备选**：让 AI 直接改三个文档（快，但会写进幻觉与错误结论，且面试官会怀疑文档真实性）；完全手工记录（真实但费时且容易漏）。
- **理由**：`decisions.md` 是决策档案、spec 是规格契约——被自动改写就失去可信度；而提交流水这类事实机器比人可靠。
- **代价**：草稿需要人工过一遍；带来一个额外产物目录需要管理。
- **反馈**：`scripts/sync-docs.mjs` 已实现；沉淀失败只写日志、不影响提交。
  踩坑留档：自动沉淀提交本身又触发了 post-commit，产生一串级联的 `docs: 自动沉淀` 提交（历史里保留，不重写）。
  修法是双保险——`post-commit` 与 `sync-docs` 各判一次"机器提交不触发沉淀"；机器提交也照样写正文、不绕开校验。

## ADR-011 · 引入 OpenSpec 做规格驱动开发

- **日期**：2026-09-22
- **状态**：已接受
- **背景**：需求方要求用 OpenSpec 初始化项目，并让后续改动及时沉淀到 spec 文档。
- **决策**：`openspec init`（v1.13.1，schema `spec-driven`，产物语言 zh-CN），生成 Claude Code / Cursor / Codex / GitHub Copilot / 通用 agents 五套指令；开发按 propose → apply → archive 流程，每个模块抽一个能力规格。
- **备选**：只在 `docs/` 手写规格；用 GitHub Issues 当需求台账；不引入任何规格层。
- **理由**：规格与变更分离（`specs/` 是现状、`changes/` 是提案），归档时会自动更新主规格，比手写文档更不容易失真；且能同时服务多个 AI 工具。
- **代价**：多一套目录与命令；规格写作有格式要求（Requirement / Scenario）。
- **反馈**：待开发阶段验证——重点是"三天里有没有时间真的走完一次 propose 到 archive 的完整流程"。

## ADR-012 · 文案约束接入 oil-tone

- **日期**：2026-09-22
- **状态**：已接受（原决策"用自拟契约替代缺失的 oil-tone"已由本决策取代）
- **背景**：需求方要求所有文档内容受 oil-tone 约束。首次核查时本机 `.agents/skills/` 只有 `oil-frontend`；随后需求方给出仓库地址 `github.com/oil-oil/oil-tone`。
- **决策**：克隆到 `~/.agents/skills/oil-tone`（其仓库把 skill 放在 `skills/oil-tone/` 子目录，已移到 skill 根目录以符合目录约定）；`scripts/lib/oil-tone.mjs` 负责定位并调用它的 `tone_lint.py`，pre-commit 对暂存的 Markdown 执行检查——FAIL 拦截、WARN 提示；`docs/writing-style.md` 保留为项目落地版（术语表、状态词）并注明上游。
- **备选**：把 tone_lint 的规则移植成 JS（自包含，但要跟着上游手工同步）；只在人工写文档时参考规范不做自动检查（等于没有约束力）。
- **理由**：同源调用能跟着 skill 更新走，且失败可跳过——它不在本机时项目照样能提交，不给协作者制造硬依赖。
- **代价**：依赖 python 与本机 skill 路径；换机器需要重装 skill。
- **反馈**：全量扫描 9 份文档，只有 1 处 FAIL（`decisions.md` 里的 `闭环` 属宣传黑话），已改。
  踩坑留档：接入后第一次试探提交没被拦住——`tone_lint.py` 在 Windows 上输出 CRLF，而解析函数按 `\n` 切行，行尾的 `\r` 让 `(.*)$` 永远匹配不上，检查静默失效；改成按 `\r?\n` 切行后正常。
  第二个坑：写作规则文档里"列出"禁词会被自己的规则拦下，禁词要用反引号或代码块包起来。
  单文件检查约 0.2s，全量 9 份文档不到 2s，放在 pre-commit 里不拖慢提交。

---

## ADR-013 · 样式用原生 CSS 变量，不引 Tailwind

- **日期**：2026-09-22
- **状态**：被 ADR-015 取代（2026-09-23）。当时的复评条件——"界面变多、样式开始重复时再引 Tailwind"——由外部需求触发，见 ADR-015。
- **背景**：设计系统已经把颜色、间距、圆角、动效定成 CSS 变量（`docs/design-system.md`）；应用当前只有一屏界面。
- **决策**：用手写 CSS + 变量实现，样式表单一文件；不引 Tailwind。
- **备选**：Tailwind v4（CSS-first 配置，能与设计系统变量共存）；Tailwind v3（成熟，但要额外配置文件与内容扫描）；CSS Modules（多一层文件管理）。
- **理由**：样式表不到 300 行，Tailwind 的收益（快速布局、强制一致性）在单屏界面里体现不出来，却多一个构建依赖和版本升级面；设计系统的变量本身就是约束，直接写更直观。
- **代价**：界面变多后样式复用要自己抽公共类；接手的人需要读 CSS 而不是查 Tailwind 文档。
- **反馈**：待界面数量明显增加、样式开始出现重复时复评；那时候再引 Tailwind 也不迟，现有变量可以原样保留。

---

## ADR-014 · 采用 v0.app 的三栏工作台布局与配色

- **日期**：2026-09-23
- **状态**：被 ADR-015 取代（2026-09-23）。"三栏骨架"这一条被保留下来，配色与组件配方整体换掉；"单一强调色、颜色只表达状态"仍然有效。
- **背景**：需求方给了一份 v0.app 生成的界面方案（Next.js + Tailwind + shadcn/ui），要求参考它的样式布局重构界面。原界面是单屏纵向流水线，设置、列表、动作挤在一列，信息密度低。
- **决策**：采纳它的三栏骨架（左设置 / 中队列 / 右详情）与四层底色配色；按本项目真实能力调整内容——左栏放批量设置与 AI 建议，右栏改成选中任务的媒体详情。
- **备选**：保留原单屏布局（已验证稳定，但密度低）；完全照搬 v0 产出（它的检查器里有编码器选择、CRF 滑块、音频与帧率下拉，后端根本没有这些能力，点下去只会骗用户）。
- **理由**：三栏是桌面工具的成熟形态，设置与内容分开符合用户预期；四层底色（canvas / panel / panel-soft / panel-raised）比原来的两级更适合区分面板、工作区与悬停态。
- **代价**：字号收了一档（正文 12px），可读性换信息密度；样式表从 7 KB 涨到 16 KB；引入 lucide-react 作为图标库（原计划用 Phosphor）。
- **反馈**：主按钮从"反白"改成"强调色实心"（v0 的做法）。为保证这条纪律不烂掉，把左栏的"添加视频"从主按钮降级为描边次级——全屏仍然只有"开始转换"一个主按钮。

---

## ADR-015 · 内化 EnsoCode 的设计语言，改用 Tailwind v4 + Base UI

- **日期**：2026-09-23
- **状态**：已接受。替代 ADR-013（样式用原生 CSS）与 ADR-014（v0.app 的配色与组件配方）；ADR-007 的立场条款与 ADR-014 的三栏骨架保留。
- **背景**：需求方要求"深度分析 [EnsoCode](https://github.com/J3n5en/EnsoCode)，参考它的样式排版彻底重构本项目，并把它的 UI 设计语言内化进来"。上一版界面（ADR-014）的底色分四层、主按钮是强调色实心，与 EnsoCode 不是同一套语言；两者混在一起会显得两头不靠。
- **决策**：整体换成 EnsoCode 的语言——
  ① 令牌层照抄它的语义命名与取值（OKLCH，中性色同色相 285.82，`--primary` 反白）；
  ② 技术底座跟它对齐：Tailwind v4（CSS-first，无配置文件）+ **Base UI**（不是 Radix）+ cva + tailwind-merge；
  ③ 组件层按它的配方重建（按钮/徽章/输入框/分段切换/下拉/对话框/提示/空态/进度条）；
  ④ 面层收敛到三档（`bg-background` 大面板 / `bg-muted` 及其半透明变体做嵌套与状态 / `bg-popover` 浮层），
     层级只由 1px 边框与半透明叠加表达；
  ⑤ 标题栏自绘（`frame: false`），因为窗口边框是系统灰条，会截断"所有面板同一个底色"。
- **备选**：
  - 保留纯 CSS、只把颜色和字号换成 EnsoCode 的值：改动最小，但组件配方（`active:scale-[0.97]`、`data-slot` 约定、变体表、半透明叠加）抄不过来，"内化语言"会退化成"换配色"；
  - 引入 EnsoCode 的全部原语（50 个 `ui/*.tsx`）：其中 `card`、`empty`、`meter`、`toolbar`、`sidebar` 在它自己仓库里都是零引用，抄过来就是抄来一堆没人维护的代码；
  - 引入 framer-motion 复刻它的侧栏 spring 动画：本项目只有一处动画用到，不值得多一个运行时依赖（ADR-008）。
- **理由**：设计语言说到底是"遇到一个结构时选哪种做法"的共识，只有连写法一起搬过来才能继续演化——后面加一个面板、一个状态，能直接照同类推。只对齐颜色的话，第一个新组件就会走回老路。
- **代价**：构建链多一个 Tailwind 插件与四个运行时依赖（`@base-ui/react`、`cva`、`tailwind-merge`、`clsx`）；全部标记与样式重写；端到端测试的定位锚点从类名换成 `data-slot`，`e2e/` 两个文件同步改；`docs/design-system.md` 与 `docs/ui-spec.md` 整体重写。
- **反馈**：
  - 对比度不是可以照抄的部分。上游暗色的 `--destructive`（`oklch(0.396 0.141 25.72)`）与 `--success`（`0.527 0.154 150.07`）是配白字做实心按钮用的，本项目拿它们当正文色时实测只有 1.97:1 与 4.01:1，低于 4.5:1 的硬线；换成 0.704 / 0.723 后是 6.85:1 与 8.70:1。这条已写进 `docs/design-system.md`，避免下次"对齐上游"时又改回去。
  - 尺寸也不能照抄。上游 `text-sm` 在 14px 根字号下是 12.25px，中文读得动；但它的 `badge` 最小一档在桌面宽度下是 10px，中文会糊成一团，本项目把徽章字号上调一档。
  - 底栏由"输入框 + 状态行"两条压成一条：本项目的主动作只作用于队列，照搬两条会空掉半行。
  - 保留的旧行为：`.ffshift`/`.queue-row` 那套类名换成 `data-slot`，端到端测试同步改；`npm run test:e2e` 9 条全绿（28.2s）。
  - **踩坑留档（同日修复）**：重写 `z-index.ts` 时把 `DROPDOWN(40)` 排在了 `MODAL_CONTENT(51)` 之下。
    Base UI 的下拉是 portal 到 body 上的，与对话框本体不是同一棵子树，于是专业参数面板里的
    **十二个下拉全部点不中**——能点开、能看见选项、点击落在遮罩上，表现是"参数大部分都改不了"。
    上游的 `Z_INDEX` 也是这个排序，它能用是因为调用方会显式传 `DROPDOWN_IN_MODAL`；
    本项目改成把下拉层直接定死在对话框之上，并在 `docs/design-system.md` 里写死这条规则。
    端到端测试随之加了两条：一条点下拉选值、一条填数字与文本——原来的用例只覆盖了数字输入框，
    正好漏掉整个下拉层。
- **踩坑留档（同批）**：`scripts/sync-docs.mjs` 的 `autoCommitDocs()` 执行 `git add docs`——整个目录，
  然后 `git commit`。提交的是**整个暂存区**，所以不只是文档：只要那一刻有东西被 `git add` 过，
  就会一起被卷进 `docs: 自动沉淀 <sha>` 这条提交里。
  实测被卷进去过两次不同的东西：四份文档的改写（`0b09686`），以及一个已 `git add` 待提交的
  bug 修复 `src/lib/z-index.ts` 与 `e2e/workflow.e2e.test.ts`（`c1d389d`）——后者提交信息是
  `docs: ...`，内容却是代码修复，对不上。
  与"一个提交一件事"冲突，也与"提交信息要说明改了什么"冲突。
  按 ADR-010 的先例（自动沉淀的级联提交"历史里保留，不重写"）没有回改历史。
  一处待评估的改法：`autoCommitDocs` 只 add 它自己写的那几个文件（`commitLogFile` / `testingFile` / 状态页），
  而不是整个 `docs/`，也不该把不属于它的暂存内容带进提交。
  这属于钩子自身的改动，没并在当前这两次改动里做。

---

## ADR-016 · 加亮暗两套主题，默认亮色

- **日期**：2026-09-23
- **状态**：已接受。替代 ADR-007 里"只做暗色"这一条（"单一强调色、颜色只表达状态"仍然有效），并推翻 ADR-015 里"只有暗色"那处偏离。
- **背景**：需求方要求"加上暗亮色主题切换，默认亮色"。上一版是深色单主题，理由是"少一半变量组合要维护"；现在这条省下来的成本要还回去。
- **决策**：亮暗两套，结构照 EnsoCode 的 `:root`（亮）+ `.dark`（暗），在 `<html>` 上挂/摘 `dark` 类；默认亮色，选过之后存进 `settings.json`。切换按钮在标题栏右侧、窗口控制左边。
- **备选**：
  - 三态（亮 / 暗 / 跟随系统）：桌面应用更常见，但"默认亮色"与"跟随系统"本身就冲突——系统是暗色时跟随就等于默认暗色。要做也得先改默认值，属于另一件事；
  - 只加切换不留记忆：每次启动都要重选，等于没做；
  - 用 `prefers-color-scheme` 媒体查询代替类名：无法让用户覆盖系统设置。
- **理由**：类名切换是这类方案里最少意外的一种——令牌层两套同名，组件层只在"输入框底色"这一个地方需要 `dark:` 变体；主题是一个纯字符串状态，能存、能测、能被端到端测试断言。
- **代价**：多一份令牌要同步维护；每个新组件都要在两个主题下各看一遍；`--accent` 在亮色下是 0.965 那一档，原来 `hover:bg-accent/50` 的写法在亮色里等于看不见，得改成满不透明度。
- **反馈**：
  - 对比度是两个主题各算一遍的事。上游亮色是从它的深色主题反相来的，`--warning`（2.15:1）与 `--info`（3.76:1）压在白底上都不够 4.5:1，各压暗一档后是 4.99:1 与 5.06:1。加上暗色那两处，一共四个状态值不是照抄的，数值与算法都记在 `docs/design-system.md`。
  - 首帧会闪。设置是渲染进程通过 IPC 异步读的，直接用它决定主题的话，暗色用户每次启动都会先看见一记白闪。改成主进程在建窗口**之前**读设置，把主题塞进渲染进程的 argv（`--ffshift-theme=`），`main.tsx` 在 `createRoot` 之前挂类名；窗口底色也跟着主题走。端到端测试里"重启后 `<html>` 上就是 dark"这条守的就是它。
  - 顺手补了一个截图上的坑：`FFSHIFT_SMOKE=shot` 原来用开发机的真实 `user-data-dir`，于是 README 里的图会跟着本机设置变（档位、主题都会串进去）。改成默认开一个临时 profile，只在显式传 `--user-data-dir` 时听传进来的——README 那句"图和当前代码永远一致"现在才算成立。

---

## ADR-017 · 移除产品内的 AI 参数建议

- **日期**：2026-09-23
- **状态**：已接受。替代 ADR-005；`openspec/changes/2026-09-23-remove-ai-suggest/` 是这次改动的规格提案。
- **背景**：需求方判断"AI 功能在这个项目上显得有点鸡肋"，要求彻底去掉。回看数据也支持这个判断：
  它需要用户自备 API Key 才用得上真模型；没有 Key 时退回关键词规则，把"压到 50MB 发微信"
  映射成一个档位建议——而这跟界面上已有的三档预设是同一件事，多一层包装反而让人以为背后有判断。
- **决策**：整套移除。删掉 `electron/ai.ts`、`src/lib/ai-suggest.ts`（含降级规则与解析）、
  `ffshift:ai-suggest` 这条 IPC 通道、preload 白名单里的 `suggest`、设置栏里的建议面板组件，
  以及 store 的 `applySuggestion`。`openspec/specs/convert/spec.md` 里"目标体积可直接输入"那条
  去掉"不必依赖 AI 建议"的措辞——这条需求本身保留，它本来就不依赖 AI。
- **备选**：
  - 保留功能但不做入口：代码与依赖还在，只是没人用，等于把维护成本留着；
  - 只留本地规则、去掉 LLM：规则引擎做的事与三档预设重叠，留下同样是重复；
  - 换一个更便宜的模型或加缓存：解决的是成本，不是"这个功能是否成立"。
- **理由**：这个产品的承诺是"不用懂参数也能转好视频"。档位（更清晰 / 均衡 / 更小）加目标体积输入框
  覆盖了同一批需求，而且完全确定、可测、不依赖网络与第三方接口。少一条链路就少一类失败。
- **代价**：失去"自然语言配参数"这个卖点，README 的能力清单少一条；`architecture.md` §3.3 那个
  "AI 工程能力"的展示面变窄（只剩余开发过程中的 AI 辅助，没有了产品内的 AI 功能）。
- **反馈**：
  - 移除得很干净：`src/`、`electron/`、`e2e/` 三处再搜不到 `ai-suggest` / `Suggestion` / `WORKBUDDY` 任何符号；
    typecheck 一次通过，单测从 257 条降到 235 条（删掉的正好是 22 条 AI 用例），端到端 12 条全绿。
  - **没动开发侧的 LLM**（`.ffshift/config.json` 的 `ai` 段、`scripts/lib/llm.mjs`、提交后生成文档草稿）。
    那是协作工具，不是产品功能：它读提交信息写草稿，跟用户能不能转好视频没关系。
    两者的"AI"只是重名，混在一起删掉会顺手毁掉 `docs/automation.md` 记录的那套机制。
  - `docs/git-workflow.md` 的 area 表去掉 `ai` 这一项（`scripts/lib/rules.mjs` 的 `AREAS` 同步改）——
    area 必须与目录一一对应，目录没了就不能留着。

---

## ADR-018 · 主界面与设置页分工：偏好搬进设置页，主界面只留转换

- **日期**：2026-09-23
- **状态**：已接受。实现了 ADR-007 里"界面要让人不用理解参数就能做出正确选择"这条目标，也把 ADR-017 之后空出来的位置安排清楚。
- **背景**：需求方要求"右上角加一个齿轮，切到设置界面，能配参数预设和默认输出路径；把主页的一些设置挪进去，主页专注视频格式切换"。
  当时的实际状况：顶栏塞着硬件状态、ffmpeg 完整版本串、主题按钮；左栏一半是"输出目录"和"专业参数"这类设一次就不动的项；
  专业参数还是个对话框，里面一屏多的表单要滚两遍。**"这次要转成什么"和"以后一直这样"混在同一屏。**
- **决策**：拆成两屏。主界面只放每批文件都要重新决定的（质量档位、输出格式、目标体积）；
  设置页（顶栏右上角齿轮进入，左侧分类右侧内容）放输出目录、参数预设、主题、引擎信息。
  同时把专业参数改成**持久化**——它进了设置页，就按设置的语义走。
- **备选**：
  - 设置开在独立窗口（EnsoCode 的做法）：要加第二个 HTML 入口、第二个 React 根、跨窗口同步设置，为一个单窗口工具付这些成本不划算；
  - 保留对话框、只把输出目录搬走：需求方明确点了"参数预设"要进设置，而且对话框里滚两遍表单这件事本身就该改；
  - 主界面再留一份"快速切换主题"：同一个布尔值两个入口，迟早会出现两处状态不一致的观感问题。
- **理由**：一屏只回答一个问题。用户打开这个软件是为了一次转换，不是来配参数的；
  要配参数的人愿意多点一次齿轮。而"这次用什么"与"以后一直怎样"分开之后，加新设置时也有了明确的落位规则。
- **代价**：设置多一次跳转（齿轮 → 分类）；主题从一次点击变成两次；顶栏少了一个常用按钮。
  参数持久化是行为变更：以前"参数是这次用什么，不落盘"，现在改了就记住。
- **反馈**：
  - **校验的落点必须跟着搬。** 原来参数有错就禁用对话框里的"完成"按钮，搬进设置页之后那个按钮不存在了，
    而错误提示在新的一屏上——如果主界面不拦，用户会带着非法参数点「开始转换」然后撞上 ffmpeg 的报错。
    做法：设置页顶部吸顶显示错误条，同时主界面的「开始转换」禁用、底栏写明「专业参数有误，去设置里改完再开始」。
    端到端测试专门守这条：先确认有任务时按钮可点，改坏参数后按钮变灰、底栏出现原因，改回来再恢复。
  - **顺手把设置页腾空的代价也还了**：主界面左栏的「专业参数」留作快捷入口，点了直接跳到对应分类，
    并把改动的项数标在行尾（`N 项已自定义`）——参数持久化之后，用户过两天会奇怪"怎么画质跟以前不一样"，
    这个标注就是答案。
  - `src/components/ui/` 里四个原语（对话框、徽章、快捷键块、转圈）随参数面板一起删掉：
    这个层里现在每个文件都有真实调用点。上游那五十个原语里也有一批零引用的，抄过来就是抄来一堆没人维护的代码。
  - 移除对话框之后 `Z_INDEX` 里的 `MODAL_*` 也删了，但把"下拉必须排在浮层遮罩之上"这条教训留在了
    `src/lib/z-index.ts` 的注释里——将来加对话框的人会在改那个文件时看到它。

---

## ADR-019 · 字号调的是根字号；外壳尺寸不跟着走

- **日期**：2026-09-23（同日修订过一次，见反馈第一条）
- **状态**：已接受。补完 ADR-018 留下的设置页，把 ADR-016 的主题那套"首帧即正确"的做法复用到第二个偏好上。
- **背景**：需求方要求设置页支持调全局字体大小，并指定了 10–24px 的范围与下拉的形态。
- **决策**：这个项目的尺寸是从一个根字号派生的（`html { font-size: var(--font-size-base) }`，全站 rem），
  所以"调字号"实现成**整体缩放**：10–24px 逐像素可选、默认 14。
  **但外壳尺寸不跟着缩放**：左右两栏宽度、标题栏高度、缩略图、窗口最小尺寸全部保持固定像素，
  只有内容随根字号变。字号放大而窗口偏小时，顶栏与底栏靠文字截断降级。
  另外把两档小字号从写死的 px 改成 rem 令牌（`text-label` / `text-metric`），它们要跟着放大。
- **备选**：
  - 只放大文字、间距不动：做不到。字号大了而控件高度还是原来的，文字会顶到边上；要真做得把每处 padding 都拆成两套；
  - 外壳也跟着缩放（第一版就是这么做的）：范围一放宽到 24px 就崩——21rem 的详情栏在 24px 下是 504px，
    三栏骨架要占 1600px，窗口下限被顶到 1611px，普通笔记本装不下；
  - 保持原样、让用户用系统缩放：那是整个应用窗口的缩放，连窗口边框一起放大，控制不了。
- **理由**：整体缩放是这套 rem 体系天然支持的做法，改一个数、全站一致。
  而"哪些尺寸跟、哪些不跟"必须一刀切开：跟的（文字、间距、控件高度）让界面按比例变大，
  不跟的（两栏宽度、标题栏）保证骨架在任何字号下都还是同一个骨架。
- **代价**：字号不再是纯粹的排版属性，它影响内容尺寸，所以每个新界面都得在两三个字号下各看一遍；
  24px 配最小窗口时信息密度会很低（名称与元信息被截断）——这是用户自选的组合，界面只保证不坏。
- **反馈**：
  - **修订（同日）**：第一版是四档（13/14/15/16）分段切换，并且让两栏宽度与窗口下限跟着字号缩放。
    需求方随后改成"10–24px 的下拉"——这个范围直接否掉了跟着缩放的前提（24px 下骨架要 1600px）。
    于是外壳尺寸写回固定像素、`minWindowSize` 与"存设置时撑开窗口"那套一起删掉。
    留档的原因是它说明了一个顺序问题：**先定取值范围，再定缩放策略**；反过来做就得返工一次。
  - **小字不跟缩放是最容易漏的一处**。当时为了对齐上游把标签写成 `text-[10px]`、指标写成 `text-[11px]`，
    都写死了像素。调大字号的人往往会发现正文大了、标签没大——而会去调字号的用户，恰恰是看不清这些小字的人。
    已改成两个语义令牌 `text-label` / `text-metric`（rem 值，14px 基准下正好是 10px / 11px）。
  - **固定像素不是一律要消灭的**。标题栏 44px、窗口按钮 48px 这类要和系统对齐的尺寸必须写死，
    否则缩放之后窗口控制会跟系统不一致。这条在 `docs/design-system.md` 里单独标出来了。
    顺手修掉一个漏网的：窗口按钮原先是 `w-12`（rem），16px 下会变成 56px，与系统的 46px 对不上，已改成 `w-[48px]`。
  - **截断比压缩好**。24px 配 940px 窗口时，第一版顶栏的"共 1 个文件"被压成一列一个字，
    因为 flex 子项会一路缩到 min-content。改成"文字可截断、按钮不缩"之后，它会显示成"共…"。
    端到端里两条用例守着：一条查根字号与重启后的值，一条确认 24px 下窗口下限仍是 940×600。
  - **基准 ≠ 正文字号**。基准 14px 对应正文 12.25px，界面上直接给用户看"14px"会让人以为正文就是 14px。
    设置页在字号下面报出派生的三个字号（正文 / 元信息 / 小节标题），把这件事说清楚。
  - 顺带纠正了设计文档里一处错误：缩略图原先写的是 96×54，实际是 `w-24`（84px @14）配 `h-[54px]`，
    两个数都不对，而且长宽比不是 16:9。改成 `h-[49px] w-24` 之后比例对了。

---

## ADR-020 · 加浅黑主题；表单底色改成 `--field` 令牌

- **日期**：2026-09-23
- **状态**：已接受。扩展 ADR-016 定下的亮暗两套。
- **背景**：需求方反馈"现在的暗色调太深了"，要一套偏灰的浅黑，像 IDEA 那种、长时间看不累。
- **决策**：加第三套 `dim`——底色 `oklch(0.29 0.006 285.82)`（#2B2B2E，中性深灰而不是近黑）、
  前景 `oklch(0.93 …)`（不用纯白）、边框比底色亮一档；状态色整套重新挑（在页面底与选中行底上都过 4.5:1）。
  主题切换从两态变三态，类名切换改成"先摘干净再挂"。
  同时把输入类控件的底从 `bg-background dark:bg-input/32` 改成语义令牌 `--field`。
- **备选**：
  - 直接把暗色的底提亮、只留两套主题：需求方要的是"多一个"，而且近黑有它的用途（夜里、OLED 屏）；
  - 抄一套现成的 Darcula 十六进制值：它的面板与编辑器底是两个颜色，与本项目"所有面板同一个底色、靠 1px 边框分层"的立场冲突；
  - 给浅黑只换底色、状态色沿用暗色那套：实测 `info` 压在新底色上只有 3.76:1，不过线。
- **理由**：三套主题的差值落在**底色明度**上（1.0 / 0.29 / 0.145），其余令牌按同一套规则重推一遍，
  所以组件层一行都不用改——这正是 ADR-016 把令牌与组件分开的收益。
- **代价**：多一份令牌要维护，新组件要在三套下各看一遍；状态色有四组值不能复用，改配色时要重新量对比度。
- **反馈**：
  - **`dark:` 变体跟的不是应用主题，这是个真 bug。** 输入框底色原来写的是 `bg-background dark:bg-input/32`，
    而 Tailwind 的 `dark:` 默认走 `@media (prefers-color-scheme: dark)`——跟的是**系统**配色。
    也就是说：系统为暗色的机器上选"亮色"主题，输入框会拿到暗色的底（本机系统是亮色，所以一直没暴露）。
    加第三套主题时这个问题会翻倍（要为 `dim` 再写一遍变体），所以改成 `--field` 语义令牌，
    三套各自定义一处。教训：**组件层不该出现主题条件类**，需要随主题变的值都走令牌。
  - **自动截图的主题开关从 ADR-018 之后就是坏的。** 它按 aria-label 找标题栏那个主题按钮，
    而那个按钮在 ADR-018 里撤销了（主题搬进设置页）。于是 `FFSHIFT_SMOKE_THEME=dark` 静默失效，
    README 里标着"暗色主题"的那张图其实是亮色的——**图与代码不一致的时候，"自动生成"反而更危险，
    因为它看起来是可信的**。已改成走设置页（齿轮 → 外观 → 点标签 → 返回），并支持三套主题。
    这次是三套主题各拍一张时肉眼比对发现的。
  - 浅黑的状态色：四组全部提亮一档（destructive 0.704→0.76、success 0.723→0.76、warning 0.769→0.78、info 0.623→0.75）。
    约束比原来严一档：不只要过页面底，还要过**选中行底色**（`bg-muted`）——状态标签就长在那一行上。
    按这条量，连原来的暗色主题都有个漏网的（`info` 在选中行上 4.03:1），留档待评估。

---

## 新决策模板（开发阶段复制使用）

- **日期**：
- **状态**：提议 / 已接受 / 被替代（指向新编号）
- **背景**：要解决什么问题
- **决策**：选了什么
- **备选**：还考虑了什么，为什么没选
- **理由**：一句话能讲清的核心原因
- **代价**：为了这个决定付出什么
- **反馈**：真实表现，是否打脸，如何调整
