# 测试清单与记录 · FFShift

> 对应笔试题第 7 条："项目设计目标是给人用……不要做的东西自己都用不明白甚至都不测试。"
> 开发阶段逐条执行并如实记录结果；**不允许只写"通过"**——失败与返工同样要记录。

## 1. 自动化单测（计划）

| 模块（计划路径） | 覆盖点 |
| --- | --- |
| `src/shared/ffmpeg-args` | 三档预设参数构造、输出文件名与扩展名映射、非法输入 |
| `src/shared/progress` | `key=value` 块解析、百分比 / 剩余时间计算、缺字段与异常值 |
| `src/shared/errors` | 6 类常见 ffmpeg 报错 → 人话映射、无法识别时的兜底 |
| `src/shared/format` | 字节 / 时长 / 速度 / 剩余时间的格式化 |

集成测试（真实 ffmpeg，环境缺 ffmpeg 时自动跳过）：用 lavfi 生成素材 → 按构造出的参数实际转换 → 校验输出文件存在且可被 ffprobe 读出。

## 2. 冒烟清单（手动，14 条）

| # | 用例 | 步骤要点 | 预期 | 结果 |
| --- | --- | --- | --- | --- |
| 1 | 中文路径 | 导入 `F:\测试\中文名 视频.mp4` 并转换 | 信息与缩略图正常，转换成功 | 待测 |
| 2 | 含空格路径 | 路径含空格的文件导入转换 | 同上（数组传参，不受影响） | 待测 |
| 3 | 批量 10 个 | 一次导入 10 个文件 → 开始转换 | 依次排队完成，进度 / 剩余时间正确 | 待测 |
| 4 | 转换中取消 | 转换进行中点"取消" | 立即停止，任务标记"已取消"，无残留半成品 | 待测 |
| 5 | 损坏文件 | 导入损坏 mp4 | 失败 + 人话原因 + 原始日志可展开 | 待测 |
| 6 | 重新转换 | 对失败任务点"重新转换" | 重新入队；连续失败 2 次停止并给"复制日志" | 待测 |
| 7 | 无音轨视频 | 导入无声视频转换 | 正常成功，无音频相关报错 | 待测 |
| 8 | 4K 大文件 | 转换 4K 视频，同时滚动列表 | 进度平滑，UI 不卡顿 | 待测 |
| 9 | 输出目录不可写 | 输出目录指向只读位置 | 失败并提示"输出位置不能写入" | 待测 |
| 10 | 磁盘空间不足 | 目标盘空间不足时转换 | 提示磁盘空间不足 | 待测 |
| 11 | 时长异常 | 时长字段异常/缺失的流 | 进度降级为"已处理时长 + 速度"，不显示错误百分比 | 待测 |
| 12 | 设置记忆 | 改输出目录 / 档位 → 重启应用 | 设置保留 | 待测 |
| 13 | 拖拽导入 | 拖入多个文件（含不支持的格式） | 列表正确新增，非法类型标"不支持" | 待测 |
| 14 | 完成动作 | 转换完成 | 行内"打开输出目录"可用；托盘通知一次 | 待测 |

## 3. 测试素材（不依赖下载）

```bash
# 10 秒 720p（含音轨）
ffmpeg -f lavfi -i testsrc2=size=1280x720:rate=30 -f lavfi -i sine=frequency=440 \
  -t 10 -c:v libx264 -pix_fmt yuv420p -c:a aac fixture_10s.mp4

# 5 秒 4K
ffmpeg -f lavfi -i testsrc2=size=3840x2160:rate=30 -f lavfi -i sine=frequency=440 \
  -t 5 -c:v libx264 -preset ultrafast -c:a aac fixture_4k.mp4

# 5 秒无声视频
ffmpeg -f lavfi -i testsrc2=size=1280x720:rate=30 -t 5 -c:v libx264 -pix_fmt yuv420p fixture_noaudio.mp4
```

```powershell
# 损坏文件：截取正常文件前 1MB，模拟下载中断
$bytes = [System.IO.File]::ReadAllBytes("fixture_10s.mp4")[0..1048575]
[System.IO.File]::WriteAllBytes("broken.mp4", $bytes)
```

## 4. 环境（文档阶段已核实）

| 项 | 值 |
| --- | --- |
| 系统 | Windows |
| Node / npm | v22.23.2 / 10.9.8 |
| ffmpeg / ffprobe | 8.0.1 essentials build（gyan.dev） |
| git | 2.45.1.windows.1 |

## 5. 开发期记录

| 日期 | 提交 / 版本 | 执行的用例 | 结果（含失败与返工） |
| --- | --- | --- | --- |
| 2026-09-22 | `82bf7d4` chore: 建立仓库骨架与工程规范 | 77 个文件 | 待人工确认（验证信息：npm run hooks:verify 16 条用例全绿） <!-- commit:82bf7d4 --> |
| 2026-09-22 | `bdc0c71` tool: 补上分支命名检查并修掉沉淀两处缺陷 | 10 个文件 | 待人工确认（验证信息：hooks:verify 17 条用例全绿） <!-- commit:bdc0c71 --> |
| 2026-09-22 | `7ccad2d` lib: 实现四个纯函数模块（含测试） | 8 个文件 | 待人工确认（验证信息：npm run test:unit → 4 个文件 51 条用例通过） <!-- commit:7ccad2d --> |
| 2026-09-22 | `2b976c7` lib: 补 ffprobe 解析与缩略图缓存键 | 7 个文件 | 待人工确认（验证信息：npm run test:unit → 6 个文件 70 条用例通过） <!-- commit:2b976c7 --> |
