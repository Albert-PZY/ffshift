# 测试清单与记录 · FFShift

> 对应笔试题第 7 条："项目设计目标是给人用……不要做的东西自己都用不明白甚至都不测试。"
> 开发阶段逐条执行并如实记录结果；**不允许只写"通过"**——失败与返工同样要记录。

## 1. 自动化测试（已落地）

命令：`npm run test:unit`（257 条）、`npm run test:integration`（25 条，真实调用本机 ffmpeg）、`npm run test:e2e`（17 条，驱动真实界面）。
集成测试在环境没有 ffmpeg 时整组跳过（`describe.skipIf`），不会挡住协作者。

三层测试的分工：单测盯纯逻辑与参数构造；集成测试盯 ffmpeg 跑不跑得通；端到端盯"界面上点一下，整条链路对不对"。
前两层进提交前的快速检查，端到端放在 `git push` 前——它要 40 秒并且会先构建，放提交前会让人想绕过钩子。

| 测试文件 | 覆盖点 |
| --- | --- |
| `src/lib/format.test.ts` | 字节 / 时长 / 百分比 / 速度 / 剩余时间 / 体积变化，含非法输入与越界收敛 |
| `src/lib/task-status.test.ts` | 八个状态的标签与色档、状态词与文案契约一致、活跃/结束判定 |
| `src/lib/ffmpeg-version.test.ts` | 从完整版本串里取版本号：构建标签、git 构建前缀、认不出时的兜底 |
| `src/lib/theme.test.ts` | 主题两态切换、显示名、窗口底色与令牌的对应、从 argv 认主题的兜底 |
| `src/lib/font-scale.test.ts` | 字号四档的取值与上限、像素值与档位名的对应、窗口最小尺寸按比例换算、从 argv 认档位 |
| `src/lib/progress.test.ts` | `key=value` 块解析、`out_time_ms` 实为微秒的坑、流式分块拼接 |
| `src/lib/ffmpeg-args.test.ts` | 三档预设、硬件加速、无音轨不加音频参数、目标体积反推码率、输出路径 |
| `src/lib/advanced-params.test.ts` | 参数校验（编码器区间、容器兼容、错误与警告分级）、额外参数边界、设置文件里的容错解析、按输出格式校验 |
| `src/lib/errors.test.ts` | 五类常见报错转人话、取消、兜底、输出打不开的误判 |
| `src/lib/ffprobe.test.ts` | 帧率换算、字段缺失、纯音频、可转换性判断（含 ffprobe 猜出来的假格式） |
| `src/lib/thumbnail-cache.test.ts` | 缓存键归一化、抽帧时间点上限 |
| `src/lib/settings.test.ts` | 设置解析容错：坏文件、缺字段、版本不符、非法枚举值、主题与专业参数的回落 |
| `electron/ffmpeg/probe.integration.test.ts` | 真实 ffprobe：中文空格路径、截断文件、垃圾数据、批量探测不中断 |
| `electron/ffmpeg/media.integration.test.ts` | 真实转码：抽帧与缓存命中、进度单调推进、取消并清理半成品、目标体积、损坏文件 |

另外两条端到端自检（不开界面用手点，直接跑整条链路）：

```bash
FFSHIFT_SMOKE=1 npx electron .          # 窗口能建、页面能加载
FFSHIFT_SMOKE=e2e FFSHIFT_SMOKE_FILE=<视频> FFSHIFT_SMOKE_OUTPUT=<输出> npx electron .
                                        # 渲染进程 → preload → IPC → ffmpeg 全链路
```

界面截图也是自动的，跑在临时 profile 上（不受本机设置影响）：`FFSHIFT_SMOKE=shot`，
可选 `FFSHIFT_SMOKE_THEME=dark` 切暗色、`FFSHIFT_SMOKE_SETTINGS=参数预设` 进设置页。

## 2. 冒烟清单（14 条）

状态说明：✅ 已验证 · ◐ 部分验证或只测了规则 · ⬜ 未实测

| # | 用例 | 预期 | 结果 |
| --- | --- | --- | --- |
| 1 | 中文路径 | 信息与缩略图正常，转换成功 | ✅ 集成测试用 `中文 样本 带音轨.mp4` 覆盖 |
| 2 | 含空格路径 | 同上（数组传参，不受影响） | ✅ 同一次集成测试，文件名同时含空格 |
| 3 | 批量 10 个 | 依次排队完成，进度正确 | ⬜ 未实测（队列串行逻辑已实现，未跑 10 个） |
| 4 | 转换中取消 | 立即停止，标记已取消，无半成品 | ✅ 集成测试：cancelled 且输出文件被删 |
| 5 | 损坏文件 | 失败 + 人话原因 | ✅ 集成测试：截断 mp4 被拒；垃圾数据被可转换性判断拦下 |
| 6 | 重新转换 | 重新入队；连续失败两次后停下 | ✅ 连续失败 2 次不再自动入队，行内「重试」清空计数；失败保留原始日志可展开复制 |
| 7 | 无音轨视频 | 正常成功，无音频相关报错 | ✅ 集成测试：hasAudio=false 且参数里无音频项 |
| 8 | 4K 大文件 | 进度平滑，UI 不卡顿 | ⬜ 未实测（未生成 4K 素材） |
| 9 | 输出目录不可写 | 提示"输出位置不能写入" | ◐ 规则已单测；未在真实只读目录上跑 |
| 10 | 磁盘空间不足 | 提示磁盘空间不足 | ◐ 规则已单测；未做真实磁盘写满 |
| 11 | 时长异常 | 降级显示，不出现错误百分比 | ✅ 单测：时长 null 时百分比返回 null，界面显示"进行中" |
| 12 | 设置记忆 | 重启后设置保留 | ✅ e2e：改档位与输出格式 → 重启 → 仍是「更小 / MKV」 |
| 13 | 拖拽导入 | 列表正确新增，非法类型标"不支持" | ◐ e2e 从 store 入口导入（真实拖放事件没法在自动化里造出文件对象）；拖放本身已实现（webUtils.getPathForFile） |
| 14 | 完成动作 | 打开输出目录可用；队列跑完发通知 | ◐ 两者均已实现，e2e 覆盖了转换完成链路；通知与打开目录依赖系统外壳，未做断言 |
| 15 | 输出目录 | 产物落在指定目录 | ✅ 路径拼接单测 7 条 + e2e 验证设置项持久化；原生目录选择框是系统窗口，自动点不到 |
| 16 | 历史记录 | 转换后出现记录，重启后还在 | ✅ e2e：真实转换 → 历史出现记录 → 重启 → 记录还在 |
| 17 | 专业参数 | 填了就生效，不填不影响 | ✅ 集成测试三条：缩放后 ffprobe 读回 640x360、CRF 40 产物小于 CRF 18、移除音轨后无音频流；不填时产出与以前逐字节一致（单测守着） |
| 18 | 参数校验 | 非法值拦住、无意义值只提示 | ✅ 单测 27 条 + e2e：CRF 填 99 时错误显示、主按钮禁用，改回 23 后恢复 |
| 19 | 参数预设 | 保存、应用、删除、导入导出 | ✅ e2e：存一个预设 → 重启 → 仍在；应用内置预设后参数确实填进面板。导入导出走原生文件对话框，未自动化 |
| 20 | 端到端套件 | 驱动真实界面走完整链路 | ✅ `npm run test:e2e`：9 条全绿（29.7s），每个用例独立的 user-data-dir |

## 3. 测试素材（不依赖下载）

```bash
# 10 秒 720p（含音轨）
ffmpeg -f lavfi -i testsrc2=size=1280x720:rate=30 -f lavfi -i sine=frequency=440 \
  -t 10 -c:v libx264 -pix_fmt yuv420p -c:a aac fixture_10s.mp4

# 5 秒 4K
ffmpeg -f lavfi -i testsrc2=size=3840x2160:rate=30 -f lavfi -i sine=frequency=440 \
  -t 5 -c:v libx264 -preset ultrafast -c:a aac fixture_4k.mp4

# 5 秒无声视频
ffmpeg -f lavfi -i testsrc2=size=1280x720:rate=30 -t 5 -c:v libx264 -pix_fmt yuv420p fixture_noaudio.mp4
```

```powershell
# 损坏文件：截取正常文件前 1MB，模拟下载中断
$bytes = [System.IO.File]::ReadAllBytes("fixture_10s.mp4")[0..1048575]
[System.IO.File]::WriteAllBytes("broken.mp4", $bytes)
```

## 4. 环境（文档阶段已核实）

| 项 | 值 |
| --- | --- |
| 系统 | Windows |
| Node / npm | v22.23.2 / 10.9.8 |
| ffmpeg / ffprobe | 9.0.2 essentials build（gyan.dev），2026-09-23 从 8.0.1 升级 |
| git | 2.45.1.windows.1 |

## 5. 开发期记录

> 下面带 `<!-- commit:xxx -->` 的行由提交钩子自动登记；结果列由人工核对后填写。失败与返工单独列在表末。

| 日期 | 提交 / 版本 | 执行的用例 | 结果（含失败与返工） |
| --- | --- | --- | --- |
| 2026-09-22 | `82bf7d4` chore: 建立仓库骨架与工程规范 | 77 个文件 | 待人工确认（验证信息：npm run hooks:verify 16 条用例全绿） <!-- commit:82bf7d4 --> |
| 2026-09-22 | `bdc0c71` tool: 补上分支命名检查并修掉沉淀两处缺陷 | 10 个文件 | 待人工确认（验证信息：hooks:verify 17 条用例全绿） <!-- commit:bdc0c71 --> |
| 2026-09-22 | `7ccad2d` lib: 实现四个纯函数模块（含测试） | 8 个文件 | 待人工确认（验证信息：npm run test:unit → 4 个文件 51 条用例通过） <!-- commit:7ccad2d --> |
| 2026-09-22 | `2b976c7` lib: 补 ffprobe 解析与缩略图缓存键 | 7 个文件 | 待人工确认（验证信息：npm run test:unit → 6 个文件 70 条用例通过） <!-- commit:2b976c7 --> |
| 2026-09-22 | `2706be3` media: 接入 ffprobe 与可转换性判断 | 8 个文件 | 待人工确认（验证信息：单测 76 条、集成测试 6 条（含中文空格路径、截断文件、垃圾数据）全绿） <!-- commit:2706be3 --> |
| 2026-09-22 | `4b4f10a` media: 加上缩略图抽帧与转换引擎 | 13 个文件 | 待人工确认（验证信息：单测 87 条、集成测试 15 条（真实转码、取消、目标体积、缓存命中）全绿） <!-- commit:4b4f10a --> |
| 2026-09-23 | `a435dbb` ai: 接入参数建议，模型不守格式时也能给出结果 | 11 个文件 | 待人工确认（验证信息：单测 109 条（含 20 条 AI 用例）；端到端自检把三种路径都跑过） <!-- commit:a435dbb --> |
| 2026-09-23 | `9daba8f` settings: 记住档位与输出目录 | 7 个文件 | 待人工确认（验证信息：单测 117 条（含 8 条设置解析用例）） <!-- commit:9daba8f --> |

### 失败与返工（人工核对）

| 提交 | 环节 | 发生了什么 |
| --- | --- | --- |
| `2706be3` | 集成：垃圾数据导入 | 原本按"ffprobe 会报错"来设计，实测它把 64 KiB 的 0x09 数据猜成 cdg 容器的 cdgraphics 视频（300x216、还带 9.1 秒时长）并返回成功。改为"有没有视频轨 + 容器是否常见"双重判断，垃圾文件在导入阶段就被拦下 |
| `4b4f10a` | 集成：目标体积转换 | 两次失败：① 输出目录不存在时 ffmpeg 立刻退出（改成转换前自动建目录）② 错误翻译把"输出打不开"误判成"找不到输入文件"，会让用户白忙（调整规则顺序并补单测） |
| `4b4f10a` | 集成：取消任务 | 首轮失败原因是输出目录不存在，ffmpeg 直接退出，压根没进到取消逻辑；修目录问题后取消用例通过（cancelled + 半成品已删） |
| `a435dbb` | 端到端：AI 建议 | 模型两次不按 JSON 输出：一次长篇反问（要分辨率、时长、场景），一次直接给方案分析。定案：不指望模型守格式，改从回答里提取要点；体积优先取用户输入里的数字，避免把模型说的"微信限制 100MB"当成用户目标 |
| `9daba8f` | 单测：设置解析 | 手写设置文件（缺 `version` 字段）被过度保守地整份丢弃。改为只有版本号冲突才回默认，缺字段逐项补默认值 |
| `685b344` | 端到端（打包后的 exe） | 通过：探测 → 抽帧 → 转换 896ms，输出 2.2 MB；打包产物与开发环境行为一致 |

### 环境变更记录

| 日期 | 变更 | 重测结果 |
| --- | --- | --- |
| 2026-09-23 | ffmpeg / ffprobe 8.0.1 → 9.0.2（同一来源 gyan.dev essentials build，配置一致） | 集成测试 15 条全绿；应用端到端自检通过（795ms、输出 2.2 MB）。旧版本目录已删除，系统 PATH 指向新目录；改动前的系统 PATH 备份在 `G:\develop\path-backup-before-ffmpeg-9.0.2.txt` |
| 2026-09-23 | `180cc93` convert: 支持选择输出格式，编码跟随容器 | 13 个文件 | 待人工确认（验证信息：单测 139 条、集成测试 18 条全绿；端到端实测 mp4 转 webm 产出 VP9 + Opu） <!-- commit:180cc93 --> |
| 2026-09-23 | `1b0019f` convert: 支持选择输出格式，编码跟随容器 (#8) | 22 个文件 | 待人工确认（验证信息：单测 139 条、集成测试 18 条全绿；端到端实测 mp4 转 webm 产出 VP9 + Opu） <!-- commit:1b0019f --> |
| 2026-09-23 | `447ae3d` convert: 支持音频与 GIF 输出，透明素材先合成底色 | 15 个文件 | 待人工确认（验证信息：单测 165 条、集成测试 22 条全绿；alpha 合成用取像素验证（白底叠加后绿分量 > 80）） <!-- commit:447ae3d --> |
| 2026-09-23 | `165f0fb` ui: 补上输出目录、目标体积与文件夹导入 | 11 个文件 | 待人工确认（验证信息：单测 172 条全绿、typecheck 通过、界面截图已更新） <!-- commit:165f0fb --> |
| 2026-09-23 | `f6840ff` ui: 失败的原始日志可展开复制，连续失败两次就停下 | 4 个文件 | 待人工确认（验证信息：单测 172 条、集成测试 22 条全绿（新增断言：失败时 error.raw 非空）） <!-- commit:f6840ff --> |
| 2026-09-23 | `afd7fd9` convert: 专业参数的数据层、校验与参数构造 | 11 个文件 | 待人工确认（验证信息：单测 222 条全绿（新增 42 条：校验 27、构造 15）） <!-- commit:afd7fd9 --> |
| 2026-09-23 | `61ab1df` test: 预设层的边界用例 | 1 个文件 | 待人工确认（验证信息：单测 237 条全绿（新增 15 条）） <!-- commit:61ab1df --> |
| 2026-09-23 | `2c4110f` test: 真正的端到端测试（9 条，驱动真实界面） | 8 个文件 | 待人工确认（验证信息：e2e 9 条全绿（29.7s）、typecheck 通过） <!-- commit:2c4110f --> |
| 2026-09-23 | `refactor/ui-enso-language` 界面重构（内化 EnsoCode 设计语言） | 见 ADR-015 | 待人工确认（验证信息：单测 251 条全绿、typecheck 通过、`npm run build` 通过、e2e 9 条全绿（28.2s）；截图重拍，5 个中间态（待转换 / 转换中 / 已完成 / 历史 / hover）人工看过） |
| 2026-09-23 | `fcda2bd` lib: 抽出状态、格式与版本号的纯函数 | 9 个文件 | 待人工确认（验证信息：单测 251 条全绿（新增 14 条）） <!-- commit:fcda2bd --> |
| 2026-09-23 | `bbc881b` ui: 用 EnsoCode 的语言重写界面 | 22 个文件 | 待人工确认（验证信息：typecheck、build 通过；五个中间态（待转换 / 转换中 / 已完成 / 历史 / ho） <!-- commit:bbc881b --> |
| 2026-09-23 | 修复参数面板（下拉层被对话框遮罩盖住）+ 亮暗主题 | 见 ADR-016 | 待人工确认（验证信息：单测 257 条全绿、typecheck 通过、build 通过、e2e 12 条全绿（40.1s，新增 3 条：对话框内下拉选值 / 数字与文本输入 / 主题切换与重启记忆）；诊断脚本逐个驱动 19 个控件，改到的 store 字段全部正确；亮暗两套主题各自截图人工看过） |
| 2026-09-23 | `62726ec` ui: 加亮暗两套主题，默认亮色 | 22 个文件 | 待人工确认（验证信息：单测 257 条全绿、typecheck 通过、build 通过、e2e 12 条全绿（新增 1 条） <!-- commit:62726ec --> |
| 2026-09-23 | 移除产品内的 AI 参数建议 | 见 ADR-017 | 待人工确认（验证信息：`src/` `electron/` `e2e/` 三处搜不到 `ai-suggest` / `Suggestion` / `WORKBUDDY` 任何符号；typecheck 通过、build 通过、单测 235 条全绿（减少的 22 条正好是删掉的 AI 用例）、e2e 12 条全绿；界面截图重拍） |
| 2026-09-23 | `6831d93` ui: 移除 AI 参数建议 | 15 个文件 | 待人工确认（验证信息：src/、electron/、e2e/ 三处搜不到 ai-suggest / Suggestion ） <!-- commit:6831d93 --> |
| 2026-09-23 | 新增设置页，主界面只留转换 | 见 ADR-018 | 待人工确认（验证信息：typecheck 通过、build 通过、单测 245 条全绿（新增 10 条：参数容错解析与按输出格式校验）、e2e 15 条全绿（58.3s，新增 3 条：设置页导航与返回、参数有误挡住开始转换、参数改动落盘）；设置页四个分类与主界面各自截图人工看过） |
| 2026-09-23 | `313c386` lib: 专业参数可持久化，校验改按输出格式 | 5 个文件 | 待人工确认（验证信息：单测 245 条全绿（新增 10 条）） <!-- commit:313c386 --> |
| 2026-09-23 | `f08593f` ui: 新增设置页，主界面只留转换 | 17 个文件 | 待人工确认（验证信息：typecheck、build 通过；单测 245 条全绿；e2e 15 条全绿（57.3s）；） <!-- commit:f08593f --> |
| 2026-09-23 | 设置页加界面字号档位 | 见 ADR-019 | 待人工确认（验证信息：typecheck 通过、build 通过、单测 254 条全绿（新增 9 条：档位取值、窗口下限换算、argv 解析）、e2e 17 条全绿（66.6s，新增 2 条：字号记忆与重启、窗口下限随字号变化）；四档字号各截图人工看过；940×600 + 16px 的挤压问题在窗口下限跟着缩放后消除，实测撑到 1076×689） |
| 2026-09-23 | `fe0d1f1` settings: 界面字号档位与窗口下限 | 5 个文件 | 待人工确认（验证信息：单测 254 条全绿（新增 9 条）） <!-- commit:fe0d1f1 --> |
| 2026-09-23 | 字号改成 10–24px 下拉，外壳尺寸不再跟着缩放 | 见 ADR-019 修订 | 待人工确认（验证信息：typecheck 通过、build 通过、单测 258 条全绿（新增 13 条：范围/收敛/派生字号/argv）、e2e 17 条全绿（64.3s）；10/14/18/24px 各截图人工看过；940×600 + 24px 由"压成一列一个字"改成文字截断降级，窗口下限恒为 940×600） |
| 2026-09-23 | `0b1883b` ui: 字号改成 10–24px 下拉，外壳尺寸不再跟着缩放 | 16 个文件 | 待人工确认（验证信息：typecheck、build 通过；单测 258 条全绿；e2e 17 条全绿（64.3s）；） <!-- commit:0b1883b --> |
| 2026-09-23 | 加浅黑主题；拆掉 `dark:` 变体；修好失效的截图开关 | 见 ADR-020 | 待人工确认（验证信息：typecheck、build 通过；单测 257 条全绿（主题三态、窗口底色等价值、argv 兜底）；e2e 17 条全绿（68.3s，主题用例扩到三套并断言类名不串台）；三套主题各截图人工看过，顺带发现并修掉"标着暗色其实是亮色"那张 README 图） |
| 2026-09-23 | `fa64163` ui: 加浅黑主题，表单底色改成语义令牌 | 10 个文件 | 待人工确认（验证信息：typecheck、build 通过；单测 257 条全绿；e2e 17 条全绿（68.3s，主题用） <!-- commit:fa64163 --> |
| 2026-09-23 | 默认值定成浅黑 + 15px | 见 ADR-021 | 待人工确认（验证信息：typecheck、build 通过；单测 258 条全绿（默认值断言更新）；快速冒烟通过；三套主题与设置页截图重拍。**端到端套件本轮按需求方要求未执行**，对默认值的断言已更新，下次跑套件时一并验证） |
| 2026-09-23 | `ac40630` settings: 默认字号 15px、默认主题浅黑 | 8 个文件 | 待人工确认（验证信息：typecheck、build 通过；单测 258 条全绿；一次快速冒烟通过） <!-- commit:ac40630 --> |
| 2026-09-23 | `0b1883b` ui: 字号改成 10–24px 下拉，外壳尺寸不再跟着缩放 | 16 个文件 | 待人工确认（验证信息：typecheck、build 通过；单测 258 条全绿；e2e 17 条全绿（64.3s）） <!-- commit:0b1883b --> |
| 2026-09-24 | 拖入接受多来源；托盘常驻与关闭确认 | 见 ADR-023 | 待人工确认（验证信息：typecheck、build 通过；单测 271 条全绿（新增 13 条拖入解析）；**e2e 22 条全绿**（新增 5 条：拖放常规/URI/无路径提示、关闭确认与记住、重启后按记住的走）） |
