## 1. 规范落地

- [x] 六条要求写进规格：注释、分层、类型、错误、依赖、命名
- [x] 每条配可检验的场景，避免无法判断的表述

## 2. 按规范复核现有代码

- [x] 删除零引用的 `uniqueOutputSuffix` 与随之无用的 import
- [x] 界面层补齐注释（App.tsx、store.ts、main.tsx）
- [x] 复核 `src/lib` 与 `electron/` 的边界：纯逻辑在前者，IO 在后者
- [x] 复核空值表达：全仓统一用 null 表示未知

## 3. 后续

- [ ] 新增依赖时在 decisions.md 记一条
- [ ] 复核中发现的问题按模块补，不一次性大改（避免影响稳定性）
