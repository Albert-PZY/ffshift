## 1. 数据层

- [x] 字号改成 10–24 的像素值，带收敛与越界处理
- [x] 旧档位名映射成像素值
- [x] 去掉 `minWindowSize` 与存设置时撑开窗口的那段

## 2. 布局

- [x] 左右两栏宽度、缩略图写回固定像素
- [x] 顶栏与底栏：文字可截断、按钮不缩

## 3. 界面

- [x] 字号改成下拉，附派生字号预览

## 4. 验证

- [x] `npm run typecheck`
- [x] `npm run test:unit`
- [x] `npm run build`
- [x] `npm run test:e2e`
- [x] 10 / 14 / 18 / 24px 各截图；940×600 + 24px 确认只是截断、没有压成一列一个字

