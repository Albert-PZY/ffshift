## 1. 数据与校验

- [x] 参数模型 20 项，全部可用 null 表示不干预
- [x] 校验：CRF 区间随编码器变、容器兼容按系族判、范围检查、额外参数边界
- [x] 错误与警告分级
- [x] 选项表（编码器、速度、tune、profile、像素格式、缩放算法、音频编码、采样率）单一来源

## 2. 参数构造

- [x] 优先级：专业设置 → 目标体积 → 容器固定编码器 → 档位
- [x] 不填时产出与以前逐字节一致（有测试守着）
- [x] 画面滤镜与 alpha 合成共存（并进 filter_complex）
- [x] faststart 覆盖容器默认，关闭时不留下孤立的 -movflags

## 3. 预设

- [x] 7 个内置预设
- [x] 用户预设：保存、应用、删除、重名加序号
- [x] JSON 导入导出，同名跳过，数量上限

## 4. 界面

- [x] 参数面板：分组表单、预设芯片、实时校验、存为预设
- [x] 有错时主按钮禁用
- [x] 左栏入口按钮
- [x] 截图自检支持 FFSHIFT_SMOKE_PANEL

## 5. 验证

- [x] 单测：校验 27 条、参数构造 15 条、预设 15 条
- [x] 集成测试：缩放落到画面、CRF 影响体积、移除音轨生效
- [x] 面板截图（docs/screenshots/advanced-panel.png）
- [ ] 预设的端到端读写（保存 → 重启 → 还在）未自动验证：e2e 走主进程，不经过界面

## 6. 顺带修的 bug

- [x] `.menu` 样式缺失：档位与输出格式的下拉一直是浏览器默认的浅色框
