## 1. 令牌层

- [x] 加 `.dim` 一套完整令牌（底色、前景、三档面、边框、状态色）
- [x] 状态色按新底色重挑，页面底与选中行底都过 4.5:1
- [x] 输入类控件的底抽成 `--field` 语义令牌，去掉 `dark:` 变体

## 2. 主题切换

- [x] `Theme` 加 `dim`，类名切换从 toggle 改成"先摘干净再挂"
- [x] 设置页主题分段切换变三项，按明度排序
- [x] 窗口底色补 `dim` 的等价值

## 3. 顺手修

- [x] 自动截图的主题开关改走设置页（原来按 aria-label 找标题栏按钮，那个按钮已经不存在了）
- [x] 用新开关重拍三套主题的截图

## 4. 验证

- [x] `npm run typecheck`
- [x] `npm run test:unit`
- [x] `npm run build`
- [x] `npm run test:e2e`
- [x] 三套主题各截图人工过一眼；确认浅黑确实比暗色"偏灰、不刺眼"

