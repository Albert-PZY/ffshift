## 1. 设置页骨架

- [x] 顶栏右上角齿轮入口；设置页里换成返回按钮
- [x] 左侧分类导航（输出 / 参数预设 / 外观 / 关于）+ 右侧内容区
- [x] 设置页与转换界面不同屏：设置页里没有转换栏、队列与详情栏

## 2. 四个分类

- [x] 输出：默认输出目录 + 恢复默认
- [x] 参数预设：预设管理（内置 / 我的 / 存为预设 / 导入导出）+ 四组参数表单 + 校验显示
- [x] 外观：主题（亮 / 暗）
- [x] 关于：应用版本、ffmpeg 版本、硬件加速 + 重新检测

## 3. 数据层

- [x] `advanced` 进设置文件（版本 5），带容错解析
- [x] 参数改动即时落盘
- [x] `validateForFormat` 纯函数：设置页与主界面问同一个问题

## 4. 主界面收敛

- [x] 左栏去掉输出目录，只留档位 / 输出格式 / 目标体积
- [x] 顶栏去掉主题按钮与版本文字，只留圆点 + 短标签 + 齿轮
- [x] 参数有误时禁用「开始转换」，底栏说明原因
- [x] 左栏「专业参数」跳到设置对应分类，并标出改了几项

## 5. 验证

- [x] `npm run typecheck`
- [x] `npm run test:unit`
- [x] `npm run build`
- [x] `npm run test:e2e`
- [x] 界面截图重拍（含设置页）

