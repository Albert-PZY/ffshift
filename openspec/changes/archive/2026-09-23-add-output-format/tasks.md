## 1. 容器与编码

- [x] 容器矩阵：MP4 / MKV / MOV / WebM 各自的编码器与封装参数
- [x] WebM 不收 H.264：强制 VP9 + Opus，并忽略硬件加速
- [x] MP4 / MOV 带 `+faststart`，Matroska 不带（不是它的选项）

## 2. 路径与参数一致性

- [x] 输出路径按目标格式给扩展名
- [x] 输出扩展名优先于格式参数（ffmpeg 按文件名选封装器）

## 3. 连带修复

- [x] 转换失败也清理半成品（此前只有取消才清）
- [x] 错误翻译覆盖"容器不收这个编码"，不再落到未知错误

## 4. 验证

- [x] 单测：容器矩阵、扩展名解析、无音轨、硬件加速冲突
- [x] 集成测试：webm → mp4、webm 保持 webm、mkv → mp4
- [x] 端到端（走 IPC）：mp4 → webm 产物为 VP9 + Opus
- [x] 界面：新增输出格式选择并持久化，截图已更新
