/**
 * 层级令牌。
 *
 * 分散在各处的 z-index 迟早会打架（"我再加个 1"），所以集中在这里。
 * 取值之间留出空档，方便插入新的中间层而不用重排。
 *
 * **下拉必须排在任何浮层遮罩之上。** 这条是踩出来的（ADR-015）：Base UI 的下拉
 * portal 到 body 上，与对话框本体不是同一棵子树——排在遮罩下面就会被盖住，
 * 症状是"能点开、能看见选项、就是点不中"，一坏就是整屏的下拉一起坏。
 * 本项目当前没有对话框（设置页替代了它），将来加的时候记得把遮罩与本体
 * 分别放在 DROPDOWN 之下。
 */
export const Z_INDEX = {
  /** 常规内容 */
  BASE: 0,
  /** 下拉、菜单、气泡 */
  DROPDOWN: 60,
  /** 全局投放提示这类压住整屏的浮层 */
  OVERLAY: 90,
  /** 提示气泡 */
  TOOLTIP: 100,
} as const;
